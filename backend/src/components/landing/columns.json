{
  "collectionName": "components_landing_column_sections",
  "info": {
    "displayName": "Spalten",
    "description": "Abschnitt mit bis zu drei individuell befüllbaren Spalten."
  },
  "attributes": {
    "anker": {
      "type": "string"
    },
    "überschrift": {
      "type": "string"
    },
    "überschriftStufe": {
      "type": "enumeration",
      "enum": ["h2", "h3", "h4"],
      "default": "h2"
    },
    "darstellung": {
      "type": "enumeration",
      "enum": ["box", "plain"],
      "default": "plain"
    },
    "spalten": {
      "type": "component",
      "repeatable": true,
      "component": "landing.column",
      "min": 1,
      "max": 3
    },
    "hintergrundfarbe": {
      "type": "enumeration",
      "enum": [
        "neutral",
        "black",
        "wine-blue",
        "wine-blue-light",
        "wine-blue-lighter",
        "wine-blue-lightest",
        "wine-blue-dark",
        "wine-blue-darker",
        "wine-blue-darkest"
      ]
    }
  }
}
